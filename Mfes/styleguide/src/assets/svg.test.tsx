import { render } from "@testing-library/react";
import CalendarIconDemo from "./svg.js";

describe("CalendarIconDemo component", () => {
  test("renders without error", () => {
    render(<CalendarIconDemo />);
  });
});
